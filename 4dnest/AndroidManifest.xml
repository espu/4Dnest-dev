<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.fourdnest.androidclient"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-sdk android:minSdkVersion="7" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:name="FourDNestApplication" >
        <activity
            android:label="@string/app_name"
            android:name=".ui.MainTabActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ui.NewEggActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ui.ViewEggActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ui.ListStreamActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="@string/app_name"
            android:name=".ui.CreateScreenActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
        </activity>
        <activity 
            android:name=".ui.PrefsActivity"
            android:label="@string/app_name">
        </activity>
        <service android:name=".services.SendQueueService" android:enabled="true"/>
        <service android:name=".services.RouteTrackService" android:enabled="true"/>
        <service android:name=".services.TagSuggestionService" android:enabled="true"/>
        <service android:name=".services.StreamReaderService" android:enabled="true"/>
    </application>

</manifest>